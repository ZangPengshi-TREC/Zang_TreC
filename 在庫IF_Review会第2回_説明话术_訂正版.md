# TRIAL ↔ Hitluster IF連携 Review会（第2回・訂正版）
# 在庫IF分析 説明话术（4資料 口播稿）

> ⚠️ **訂正のお知らせ**：旧版（`在庫IF_Review会第2回_説明话术.md`）はTRIAL側テーブルを
> `current_stock_data`（id/store_code/jan/quantity/amount/last_updated_at,
> UNIQUE(store_code,jan)想定）として整理していましたが、これは**No.51/No.52の対象テーブルとして
> 誤り**でした。実際にNo.51/No.52が対象とすべきTRIAL側テーブルは **`center_stock`** で、
> カラム構成が大きく異なるため、本資料で全面的に再整理しました（訂正日：2026年8月11日）。
>
> **【追記】`current_stock_data`自体は実在するテーブルです。** その後の追加調査で、
> 同名のテーブル設計書（DDL＋実データ）を確認しました。備考に「仕入れ・売上・廃棄・振替等で
> 更新する」とあり、**店舗（店CD）単位の現在庫テーブル**と推測されます。一方、No.51/No.52の
> Hitluster側キー項目は**「倉庫コード」**であり店舗コードとは概念が異なるため、本IFが対象とすべき
> なのは店舗単位のcurrent_stock_dataではなく、センター/倉庫単位と推測される`center_stock`の方だと
> 判断しています。ただし最終的な役割分担（本当に倉庫/センター単位で別テーブル管理なのか）は
> TRIAL側に確認が必要な論点として、GAP分析書・QA一覧（QA-G01/QA-S01）に追記しました。
>
> 説明順：**No.51 在庫調整データ → No.52 在庫一覧データ**
> 対象テーブル：`center_stock`（両IFが同一テーブルに書き込む想定）

---

## center_stock 実際の構造（旧版との対比）

| 列名 | 型 | 旧版`current_stock_data`との対比 |
|------|----|-----------------------------------|
| id | BIGSERIAL PK | 同名・変更なし |
| stock_date | TIMESTAMP | **新規**（旧版になし） |
| operation_time | TIMESTAMPTZ | **新規**（旧版になし） |
| store_code | INT4 | 同名・変更なし（実データ例:580） |
| product_code | VARCHAR(13) | 旧`jan` VARCHAR(20)から**改名+桁数縮小** |
| quantity | NUMERIC | 旧NUMERIC(13,4)から精度表記が変更（要確認） |
| transfer_time | TIMESTAMPTZ | **新規**（旧版になし） |
| registered_at | TIMESTAMPTZ | 旧`last_updated_at`に相当か要確認 |
| registered_by | VARCHAR(20) | **新規**（旧版になし、実データ例:"Migration"） |
| ~~amount~~ | ~~NUMERIC(13,4)~~ | **削除**（移動平均単価の格納先が消滅） |

**最大の変更点は3つです。**
1. `amount`（移動平均単価）の格納先が完全に消滅した
2. `last_updated_at` 1列だったのが、`stock_date`/`operation_time`/`transfer_time`/`registered_at`の4列に分かれ、用途が未確定
3. 旧版が前提としていた `UNIQUE(store_code, jan)` 制約が確認できず、**center_stockが「履历追加型（ログ）」の可能性が高い**（現在値を1行で保持する設計ではない可能性）

> 補足：`current_stock_data`は店舗単位の別テーブルとして実在しており、`amount`列自体は
> そちらにも「未使用フィールド」として存在していました。つまり移動平均単価は旧版の時点でも
> 実質使われていなかった項目ですが、center_stockでは列自体が存在しない点が新たな確認事項です。

**【所要時間の目安】約1分**

---

## 全体の導入（冒頭30秒）

> 本日は第2回Review（訂正版）として、**在庫IF**の項目マッピング表とGAP分析書、計4種×2IF＝8資料をご説明します。
>
> 対象は以下の2IFです。
> 1. **No.51 在庫調整データ**（データ区分 `"32"`、差分型・随時送信）
> 2. **No.52 在庫一覧データ**（データ区分 `"31"`、全件スナップショット・毎日22:00）
>
> いずれもTRIAL側の **`center_stock`** に格納する想定です。
>
> 本日のゴールは、①旧版で参照していたテーブルが誤りだったことのご報告、②center_stockの
> **書き込み方式（履历追加型か現在値型か）の確定**、③倉庫コード→店舗コードなど優先度「高」の
> GAPの方針決定です。

**【所要時間の目安】約30秒**

---

## 資料1：No.51 在庫調整データ_マッピング表（訂正版）

**ファイル名：** `_在庫IF分析_No51_在庫調整データ_マッピング表_訂正版.xlsx`

### 説明话术

> 1つ目は、**No.51 在庫調整データ**の項目マッピング表です。前回資料からTRIAL側の参照テーブルを
> `current_stock_data`から`center_stock`に訂正しています。
>
> Hitluster側は固定長レコード（データ区分`"32"`）で、在庫調整が発生したタイミングで**差分型**に
> 送信されます。ただし、center_stock側にUNIQUE制約が確認できておらず、旧版前提の「差分UPSERT」が
> そのまま成立するか不明です。
>
> 主なマッピングは以下の通りです。
>
> | Hitluster | TRIAL(center_stock) | 判定 |
> |--------|-------|------|
> | 倉庫コード(6桁文字列) | store_code (INT4) | **変換必要** |
> | 商品コード(14byte) | product_code (VARCHAR13) | **変換必要**（TRIM、桁余裕消失） |
> | 符号区分+在庫調整数 | quantity | **要決定**（書込方式未確定） |
> | 移動平均単価 | — （対応列なし） | **対応なし**（GAP拡大） |
> | 在庫調整日付 | stock_date / operation_time | **確認必要**（4時刻列に分散） |
> | 在庫調整コード | — | **確認必要**（別テーブル要否） |
> | データ区分 / FILLER | — | 不要（破棄） |
> | （新規）— | registered_by | **確認必要**（新規項目・値未定義） |
>
> ポイントは4つです。
>
> 1. **書き込み方式が未確定（最重要・新規）**
>    center_stockにUNIQUE制約が見当たらず、履历追加型（毎回新規行）の可能性が高いです。
>    その場合、符号区分＋調整数をどう反映するか3つの案があります。
>    - プランA：直近の最新quantityを読み取り、加減算した「調整後の総量」を新規行としてINSERT
>    - プランB：差分量そのものを新規行としてINSERT、参照側でSUM集計
>    - プランC：在庫調整専用の別テーブルに分離
>
> 2. **倉庫コード→店舗コード**
>    Hitlusterは西友倉庫コード（例`"007452"`）。実データではstore_codeに580のような小さい数値が
>    入っており、変換テーブルが必要な点は旧版と同じです（QA-G01）。
>
> 3. **移動平均単価の格納先が消滅**
>    旧版は「未使用フィールドあり」でしたが、center_stockには列自体が存在しません。
>    恒久的に破棄でよいか、優先度を上げて再確認したいです（QA-G04）。
>
> 4. **4つの時刻列の用途が未確定**
>    stock_date/operation_time/transfer_time/registered_atのどれにどう値を入れるか、
>    暫定案は記載していますが正式な定義確認が必要です（QA-S02）。

### Review会で確認したい質問例

- 「center_stockに(store_code, product_code)のようなUNIQUE制約は存在しますか？履历追加型で正しいでしょうか？」
- 「倉庫コード→店舗コードの対応表は、どこで管理・提供いただけますか？」
- 「移動平均単価は本当に格納不要でしょうか？（列自体が存在しません）」
- 「stock_date/operation_time/transfer_time/registered_atはそれぞれどのように使うべきでしょうか？」

**【所要時間の目安】約4分**

---

## 資料2：No.51 在庫調整データ_GAP分析書（訂正版）

**ファイル名：** `_在庫IF分析_No51_在庫調整データ_GAP分析書_訂正版.xlsx`

### 説明话术

> 2つ目は、No.51の**GAP分析書**です。全10件、優先度の高いものからご説明します。
>
> **① 【訂正】TRIAL側参照テーブルの対象誤り（★最高・新規）**
> 旧版は`current_stock_data`を参照していましたが、これは実在するものの店舗（店CD）単位の現在庫
> テーブルと推測され、倉庫コード基準の本IFとは対象が不一致でした。本資料以降、センター/倉庫単位
> と推測される`center_stock`を正として全面訂正しています。ただし店舗単位テーブルとの役割分担は
> 最終確認事項として残っています（QA-G01）。
>
> **② 書き込み方式未確定（★最高・新規）**
> UNIQUE制約が確認できず、履历追加型の可能性が高いです。プランA/B/Cのいずれかを確定する必要が
> あります（QA-S01）。これは本日最優先でご判断いただきたい論点です。
>
> **③ マッピング不足：倉庫コード→店舗コード（★高）**
> 旧版と同じ論点です。業務側での対応表定義が必須です（QA-G01）。No.52と共通ルールです。
>
> **④ 移動平均単価の格納先消失（★高・優先度UP）**
> 旧版は「未使用列あり」でしたが、列自体が存在しなくなりました。恒久的に破棄でよいか再確認
> したいです（QA-G04）。
>
> **⑤ 商品コードの桁オーバーフローリスク上昇（★中）**
> 旧VARCHAR(20)からVARCHAR(13)ちょうどに変わり、桁余裕がなくなりました。13桁超のコード受信時の
> エラーハンドリングを新たに検討する必要があります（QA-G07）。
>
> **⑥ 時刻列の割当未確定（★中）**
> 4つの時刻列の用途定義が必要です（QA-S02）。
>
> **⑦ registered_by設定値未定義（★中・新規）**
> 実データでは"Migration"という値が入っており、本IF由来のレコードにどの値を設定するか
> 新たに定義が必要です（QA-S03）。
>
> **⑧ TRIAL側未対応：在庫調整コード（★中）**
> 別テーブル記録の要否（QA-A01）。変更なし。
>
> **⑨ 排他制御・重複防止（★中）**
> UNIQUE制約の有無と合わせて重複防止方式を定義する必要があります（QA-G06）。
>
> 低優先度は、データ区分/FILLER破棄で、技術対応可能です。
>
> 本日は特に **書き込み方式の確定** と **移動平均単価の恒久的な扱い** のご判断をお願いします。

### Review会で確認したい質問例

- 「center_stockは履历追加型で正しいか、それとも別途UNIQUE制約が存在するか？」
- 「符号02(−)で対象データが見つからない場合、初期値はどう扱いますか？」

**【所要時間の目安】約3.5分**

---

## 資料3：No.52 在庫一覧データ_マッピング表（訂正版）

**ファイル名：** `_在庫IF分析_No52_在庫一覧データ_マッピング表_訂正版.xlsx`

### 説明话术

> 3つ目は、**No.52 在庫一覧データ**の項目マッピング表です。No.51と同様、参照テーブルを
> `center_stock`に訂正しています。
>
> Hitluster側はデータ区分`"31"`で、**毎日22:00に全件スナップショット**（サンプル3545行）を
> 送信します。
>
> No.51との違いは、**quantityが差分ではなく絶対値**である点、加えて**書き込み方式によって
> 反映方法が大きく変わる**点です。
>
> - center_stockが**履历追加型**の場合：当日分をそのまま新規行として一括INSERT。
>   ただし日々レコードが積み上がるため、**保管期間・パーティション等のデータ量対策**が
>   新たに必要になります（QA-S04）。
> - center_stockが**現在値型**の場合：(store_code, product_code)にUNIQUE制約を新設し、
>   DELETE→INSERTまたはUPSERTで反映（旧版と同様の論点）。
>
> 在庫数0が63.4%（2248/3545行）ある点、移動平均単価の格納先が消滅している点はNo.51と共通です。

### Review会で確認したい質問例

- 「center_stockが履历追加型だと仮定した場合、日次で行数が増え続けても問題ないでしょうか？保管期間の方針はありますか？」
- 「在庫数0のレコードはTRIAL側に格納すべきでしょうか？」

**【所要時間の目安】約4分**

---

## 資料4：No.52 在庫一覧データ_GAP分析書（訂正版）

**ファイル名：** `_在庫IF分析_No52_在庫一覧データ_GAP分析書_訂正版.xlsx`

### 説明话术

> 最後は、No.52のGAP分析書です。全11件です。
>
> **① 【訂正】TRIAL側参照テーブル誤り（★最高）** No.51と同じ訂正内容です。
>
> **② 書き込み方式未確定（★最高）** No.51と共通論点です（QA-S01）。
>
> **③ マッピング不足：倉庫コード（★高）** No.51と同一論点です。
>
> **④ 履历追加時のデータ量増大（★高・新規）**
> 履历追加型の場合、1日あたり約3545行×365日/年でテーブルが急速に肥大化します。
> パーティショニング・アーカイブ方針の検討が必要です（QA-S04）。
>
> **⑤ 移動平均単価の格納先消失（★高・優先度UP）** No.51と共通です。
>
> **⑥ IF間整合性：No.51とNo.52の適用順序（★中・内容更新）**
> 書き込み方式の結論により論点が変わります。現在値型なら従来通り適用順序の定義が必須ですが、
> 履历追加型なら「どのIF由来の行を最新として参照するか」という判定ルールの定義に変わります。
>
> **⑦ 在庫数=0レコードの扱い（★中・変更なし）**
> **⑧ 欠損時の挙動（★中・変更なし）**
> **⑨ 商品コード桁オーバーフローリスク／⑩ 時刻列割当／⑪ registered_by未定義** はNo.51と共通の
> 中優先度論点です。
>
> 本日は特に **書き込み方式の確定** と **履历追加時のデータ量対策** のご判断をお願いします。

### Review会で確認したい質問例

- 「通常運用で、調整（No.51）と一覧（No.52）はどちらを最終真実とみなしますか？」
- 「履历追加型の場合、center_stockのデータ保管期間はどの程度を想定すべきでしょうか？」

**【所要時間の目安】約3.5分**

---

## クロージング（締めの话术）

> 以上、在庫IF 4種8資料（訂正版）のご説明でした。
>
> 本日ご判断いただきたい論点を整理します。
>
> **【最優先・訂正報告】**
> 1. TRIAL側参照テーブルの訂正確認（対象を`current_stock_data`→`center_stock`に変更。
>    なお`current_stock_data`自体は実在する別テーブルのため役割分担の確認が必要）
> 2. center_stockの書き込み方式の確定：履历追加型 / 現在値型 / その他（QA-S01）
>
> **【共通・重要】**
> 3. 倉庫コード → store_code（INT4）のマッピング定義、および`current_stock_data`との
>    役割分担確認（QA-G01）
> 4. 移動平均単価の恒久的な扱い（列が存在しないため確認・QA-G04）
> 5. 4つの時刻列（stock_date/operation_time/transfer_time/registered_at）の用途定義（QA-S02）
> 6. registered_byに設定する値の定義（QA-S03）
> 7. 商品コード桁オーバーフロー時のエラーハンドリング（QA-G07）
>
> **【No.51 在庫調整】**
> 8. 符号±の反映方式（プランA/B/C）の確定（QA-S01と連動）
> 9. 在庫調整コードの別テーブル要否（QA-A01）
> 10. 排他制御方式（QA-G06）
>
> **【No.52 在庫一覧】**
> 11. 履历追加型の場合のデータ量対策（保管期間・パーティション、QA-S04）
> 12. No.51/No.52の適用順序または最新行判定ルール（QA-B01）
> 13. 在庫数0レコードの格納要否（QA-B03）
> 14. 欠損時フォールバック（QA-B04）
>
> ご確認・ご指摘をいただければ、次回までに資料を更新いたします。よろしくお願いいたします。

**【所要時間の目安】約1.5分**

---

## 付録：会議進行の目安

| 順番 | 内容 | 所要時間 |
|------|------|---------|
| 1 | center_stock構造の訂正報告 | 1分 |
| 2 | 全体導入 | 0.5分 |
| 3 | 資料1（No.51 マッピング） | 4分 |
| 4 | 資料2（No.51 GAP） | 3.5分 |
| 5 | 資料3（No.52 マッピング） | 4分 |
| 6 | 資料4（No.52 GAP） | 3.5分 |
| 7 | 確認事項まとめ・締め | 1.5分 |
| **合計** | | **約18分**（Q&A除く） |

---

## 付録：判定・優先度の見方

| 記号/優先度 | 意味 |
|------------|------|
| 変換必要 | 型・コード・演算の変換が必要 |
| 確認必要 | 業務方針の決定が必要 |
| 要決定（新規） | 書き込み方式などアーキテクチャ上の決定が必要（未確定・最重要） |
| 対応なし（GAP拡大） | 訂正前は存在した回避策（未使用列等）が訂正後は存在しない |
| 変換不要 / 不要 | 未使用または破棄 |
| ★最高 | テーブル参照誤りの訂正・書き込み方式など根本設計に関わる |
| ★高 | キー・整合性・業務ブロッカー |
| ★中 | 処理方式・運用方針 |
| ★低 | 技術対応で完結可能 |

---

## 付録：No.51 と No.52 の対比（説明時に参照）

| 観点 | No.51 在庫調整 | No.52 在庫一覧 |
|------|---------------|---------------|
| データ区分 | `"32"` | `"31"` |
| 送信タイミング | 随時（調整発生時） | 毎日22:00 |
| quantity | 差分（符号±で加減算想定） | 絶対値（スナップショット） |
| 固有項目 | 在庫調整コード | — |
| 共通論点 | 倉庫→店舗、JAN TRIM(桁余裕消失)、amount格納先消失、4時刻列の用途、registered_by未定義 | 同左 |
| 横断論点 | 両IFが`center_stock`を更新想定 → **書き込み方式（履历追加/現在値）の確定が最優先** | 同左 |
| 新規論点 | 符号±の反映プラン（A/B/C） | 履历追加型時のデータ量対策 |

---

## 付録：QA番号 新旧対照表

| QA番号 | 内容 | 状態 |
|--------|------|------|
| QA-S01 | center_stockの書き込み方式（履历追加/現在値） | **新規・★最高** |
| QA-S02 | 4時刻列の用途定義 | **新規・★中** |
| QA-S03 | registered_byの設定値 | **新規・★中** |
| QA-S04 | 履历追加型の場合のデータ量対策 | **新規・★高**（No.52のみ） |
| QA-G01 | 倉庫コード→店舗コード | 既存・変更なし |
| QA-G04 | 移動平均単価の扱い | 既存・**優先度UP（低→高）** |
| QA-G05 | 旧：日付粒度の方針 | QA-S02に統合・解消 |
| QA-G06 | 排他制御方式 | 既存・内容更新（No.51のみ） |
| QA-G07 | 商品コード桁オーバーフロー | **新規・★中** |
| QA-A01 | 在庫調整コードの記録要否 | 既存・変更なし（No.51のみ） |
| QA-A02 | 新規行時の初期値方針 | 既存・再構成（プランにより意味変化） |
| QA-B01 | No.51/No.52の適用順序 | 既存・内容更新（No.52のみ） |
| QA-B02 | 全件反映方式（DELETE→INSERT/UPSERT） | 履历追加型の場合は論点自体が変化 |
| QA-B03 | 在庫数0レコードの扱い | 既存・変更なし（No.52のみ） |
| QA-B04 | 欠損時フォールバック | 既存・変更なし（No.52のみ） |

---

*作成日：2026年8月11日（第2回Review 訂正版）*
*訂正理由：No.51/No.52（倉庫コード基準）の対象テーブルを`current_stock_data`から`center_stock`に修正。
なお`current_stock_data`は実在するテーブル（店舗単位の現在庫と推測）であり、対象違いが訂正理由。
役割分担の最終確認はQA-G01/QA-S01にて依頼。*
