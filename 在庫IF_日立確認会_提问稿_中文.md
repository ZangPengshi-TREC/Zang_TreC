# 库存IF 日立确认会 提问稿（7分钟・照着念）

> 对象：No.153／No.164  
> 出处：项目移送表 IFSYLMS310R（库存一览）／ IFSYLMS320R（库存调整）  
> 时间：开场约1分钟 → 问题1约3.5分钟 → 问题2约2分钟 → 收尾约30秒

---

## 开场

今天想就库存IF，确认之前请教过的两点。

第一点是，库存调整数据和库存一览数据的更新频率、以及适用顺序。
第二点是，两个IF限定在同一数据日期时，数据上是否完全一致。

这两点都是 TRIAL 侧取入设计的前提。
想对照项目移送表的记载，确认实际运营情况。

---

## 问题1（No.153）更新频率与适用顺序

先确认更新频率。

按项目移送表，库存一览是全件更新，库存调整是差分。
想确认这个理解是否正确，以及实际是怎么发送的。

### ① 库存调整数据（IFSYLMS320R）

我们的理解是：库存调整数据在发生调整时随时发送，属于差分型。

实际运营中，更新频率大概是怎样的？
是每次发生都逐笔发送吗？
还是每隔几分钟、几小时汇总后发送一次？

如果有夜间汇总等例外，也请一并说明。

### ② 库存一览数据（IFSYLMS310R）

我们的理解是：库存一览数据是日次发送的全件快照。

发送时点是几点？
补充事项里写有「联动时限到20点为止」。
这个20点，是一览文件的产出时刻吗？
还是纳入入荷、出荷实绩的截止点？
请说明它与一览发送时刻的关系。

### ③ 适用顺序

两个IF预定会更新 TRIAL 侧的同一张表。
到达顺序不同，库存的最终值就会不同。

方案A：先库存调整，后库存一览。
这种情况下，日次一览作为最新值覆盖。

方案B：先库存一览，后库存调整。
这种情况下，调整差分作为最新值保留。

从 Hitluster 侧来看，最终值应以哪边为准？
如果日次发送前后刚好收到调整数据，分界怎么处理，有规定的话也请告知。

---

## 问题2（No.164）同一日期是否完全一致

接下来确认数据内容。

两个IF的数据，如果按指定日期限定后抽取，抽取结果是否完全一致？

举例：7月21日抽取7月20日的数据。
想确认7月20日这一天，下面两点是否对齐。

第一是件数。
第二是按仓库、按商品的库存数量。

是否可以认为完全一致？
如果会不一致，请说明产生差异的原因。
例如对象范围不同、是否包含库存为零、已发送实绩是否计入、抽取时刻不同，等等。

---

## 收尾

以上两点。
请就更新频率、发送时刻、适用顺序、同一日期是否一致，给出贵方看法。
收到的内容会反映到 TRIAL 侧的取入设计中。
请多关照。
