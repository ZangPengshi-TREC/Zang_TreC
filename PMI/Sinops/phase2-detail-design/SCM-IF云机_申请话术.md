# SCM 统一 IF 云机　申请话术（中文）

用途: Spec 申请时，用 1～2 分钟说明云机的功能范围。  
日文对照: `SCM-IFクラウド機_申請話術.md`  
依据: `SCM-IFクラウド機アーキテクチャ.md` / 同名 draw.io（2026-09-16）  
假称: **ProjectD / Seiyu_Order 只是资料上的例子**，不是正式 GCP 项目名。口头上要说「暂称」「举例」。也可以不提这个名字。

---

## 时间不够时（15 秒）

> 申请的是 TRIAL GCP 上新建的 1 个项目。内容只有外部 IF。从集计 SV 抽出、共通化和型转换、本机保存、和連携用 GCS 做文件进出，这就是云机。GCS 本身和西友的 DataSpider 不进这次云机 Spec。連携用 GCS 仍要向松尾另行申请。

---

## 正文（约 1 分半～2 分钟）

今天说明的是，西友 MD 基干整合里要申请的 SCM 统一 IF 云机范围。

申请对象是 TRIAL GCP 上新建的 1 个项目。资料里暂称作 ProjectD、Seiyu_Order，这只是例子，不是正式项目名。位置上，它和现有的惣菜、MD-Link、Inunaki 并列，是一台 IF 机。

云机负责的，只是见积里的 Layer①，也就是外部 IF。功能有四项。

第一，往路从集计 SV 抽出。基干数据先到集计 SV，云机从那里取出往路 IF。不是所有往路都从集计取：新商品在库在云机生成，仕入先休日走自动发注连携。发注劝告是复路，从連携用 GCS 拉取。

第二，共通化、型转换，以及本机保存。西友侧和 TRIAL 侧位数、代码不一致的地方在这里对齐，转换后的文件在云机上暂存。

第三，往路的文件输出。把转换后的文件放到連携用 GCS。

第四，复路的取得和转交。西友侧把发注劝告放到連携用 GCS，云机取回来，转成 TRIAL 自动发注 IF，再放到自动发注侧的 GCS。送到 OrdreSV 的，是现有的自动发注传送。云机不直连 OrdreSV。

周边简要说一下。Layer② 和 Layer③ 在西友 Azure 的 DataSpider。② 是需要的主数据 Intake，③ 是送到 Sinops 和 BO，以及接收劝告。不做二次加工。TRIAL 侧和西友侧之间的通道只有連携用 GCS。GCS 和 DataSpider 不进这次云机 Spec。連携用 GCS 仍要向松尾另行申请。Hinemos 从共通基盤启动：Layer① 在这个新建项目上，Layer②③ 在西友 DataSpider 上。

以上。要申请的是：做抽出、转换、保存、文件进出的、TRIAL 侧外部 IF 用的 1 个项目。

---

## 被问到再补（正文里不念）

**往路都从集计 SV 取吗。**  
不是。多数往路从集计 SV。新商品在库在云机用西友 DWH 棚割 × TRMD マスタ生成。仕入先休日的源是 TRIAL 自动发注连携。发注劝告从連携用 GCS 拉取。

**GCS 不进 Spec，是不是不用申请。**  
这次 Spec 只申请云机。連携用 GCS 不是云机项目，但仍要向松尾另行申请。DataSpider 也不进这次 Spec。

**連携用 GCS 和自动发注 GCS 是两套吗。**  
是两套。連携用 GCS 是 TRIAL 侧和西友侧之间的唯一通道。自动发注 GCS 是 TRIAL 内的既存功能，用来把劝告交给 OrdreSV。两套都不是云机这个项目。

**为什么必须是 1 个项目。**  
为了不把 SCM 统一联动面拆开。按课题各开一个项目，Hinemos 的启动对象和文件落点就会变成两套。

**资料里的 ProjectD 是正式名吗。**  
不是。只是为了和现有的 A 惣菜 / B MD-Link / C Inunaki 并列说明，用的暂称。正式项目 ID 还没定。

**BO 和共通基盤呢。**  
BO 程序本阶段不做，但会用同一套云机和 DataSpider。共通基盤的启动是枢纽侧，不是云机外部 IF 功能本身。

**Inunaki 的 PSC 呢。**  
那是网络前提。不是这次申请的云机功能范围。和桶契约的最终对应还没对上。
