# 発注勧告IF 参照先変更 Review会 説明话术

## 冒頭

> 発注勧告IFについて、比較対象が変更になりましたのでご説明いたします。
>
> 従来はCoreSaverの数理発注データと比較しておりましたが、
> 今回から `SIREJAN_RCMDORDER_LAST` を参照します。
> これは「仕入JANの最終推奨数を反映する」ためのレイアウトです。

## 新しい対象項目

> 新しい参照先は7項目です。
>
> KEYは、店舗CDの `BranchCD` と、仕入発注JANの `SireJAN` の2項目です。
> VALUEは、推奨発注数量、ベンダーCD、納品日、Ordertype、更新日です。
>
> そのため、従来資料に記載していたCoreSaver固有の
> `sets`、`cost`、`price`、`odunit_qy` 等は、今回の比較対象から外します。

## 暫定マッピング

> 店舗系の `kankoku.txt` と `subkankoku.txt` では、
> 店舗コード、商品コード、納品日を、それぞれ
> `BranchCD`、`SireJAN`、`ShippingDate` に対応させます。
>
> 推奨発注数量は、発注単位調整後のバラ総数を設定します。
> 店舗系はNo.11の発注バラ数、
> 倉庫系はNo.11のバラ換算数量を、そのまま設定します。
> いずれも発注単位の整数倍です。
> 従来のCoreSaver向けの1000倍変換は行いません。

## 重点確認事項

> 今回、特に確認したい点は5点です。
>
> 1点目は、店舗コードです。
> sinops側は実態6桁ですが、`BranchCD` は固定長4桁です。
> 正式な変換規則をご教示ください。
>
> 2点目は、ベンダーコードです。
> sinops側は実態9桁ですが、対象表は固定長8桁です。
> 8桁コードの取得元、または変換表をご確認いただきたいです。
>
> 3点目は、Ordertypeのコード定義です。
> 固定長1桁ですが、値の意味が資料に記載されていません。
>
> 4点目は、最終値の決め方です。
> 対象表のKEYは店舗CDとJANだけです。
> 一方、sinops側では同じ店舗・JANでも、発注日や納品日が異なる複数レコードが存在します。
> どのレコードを最終推奨数として残すか、優先規則の確認が必要です。
>
> 5点目は、倉庫系への適用可否です。
> 対象表の項目は店舗CDとなっておりますので、
> `ka.txt` と `subka.txt` も同じ表を使用するのか、
> 倉庫系には別レイアウトがあるのかをご確認ください。

## 締め

> 以上の確認結果を反映して、マッピング表、GAP分析書、変更ルール定義書を確定します。
> 特に、最終値の優先規則は実装結果に直接影響するため、
> 実装開始前に合意させていただきたいです。
